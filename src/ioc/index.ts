export { DIRegistry } from './registry';
export { CLIENTS, SERVICES, CONTROLLERS } from './constants';
export type { ClientKey, ServiceKey, ControllerKey } from './constants';
export { registrations } from './registrations';
export type { RegistrationEntry } from './registrations';
export { injectable, inject, container } from 'tsyringe';
